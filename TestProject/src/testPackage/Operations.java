package testPackage;

public class Operations{

  //Decrement method returns a value after decrementing it.
  public static int decrement(int x){ return x--; }
  //Increment method returns a value after incrementing it.
  public static int increment(int x) {return x++;}
  //doubleIncrement method returns a value after incrementing it twice.
  public static int doubleIncrement(int x){return (x+2);}
  
}
